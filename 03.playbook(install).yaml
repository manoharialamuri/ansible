- name: nginx
  hosts: web
  become: yes
  tasks:
  - name: install nginx
    ansible.builtin.package:
      name: nginx
      state: present

  - name: Starting nginx
    ansible.builtin.service:
      state: started
      enabled: yes

